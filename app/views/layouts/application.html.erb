<!DOCTYPE html>
<html>
<head>
  <title>Normative Press</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <style>
  	body{color: #36393D; font-family:Georgia, serif;margin-top:25px;}
  	a, a:hover, a:visited, a:active{color:#4096EE;text-decoration:none;font-size: 14px;}
  	#container{}
  	#inner_container{width:960px;}
  	#title{font-size:36px;font-weight:bold;margin-bottom:14px;}
  	#tagline{font-style:italic;}
  	#nav{width:100%; text-align:right;}
  	.post{padding-bottom: 40px;}
  	.quote{margin-bottom: 12px; font-size:16px;border-left:1px solid #DBDBDB;padding-left: 15px;}
  	.book_meta{font-size:12px;}
 		.admin_options{font-size:10px;}
  </style>
</head>
<body>
	<div id="container" align="center">
		<div id="inner_container" align="justify">
			<div id="title">Normative Press</div>
			<div id="tagline">"...relating to an ideal standard or mode..."</div>
			<div id="nav">
		  	<% if current_user %>
		    	<%= link_to "Edit Profile", edit_user_path(current_user.id)%>
		      <%= link_to "Logout", :logout%>
		    <% else %>
					<%= link_to "Login", :login %>
		    <% end %>
		  </div>
		
			<div id="content">
				<%= yield %>
			</div>
		</div>
	</div>
</body>
</html>